{
  "name": "pg-backup",
  "version": "1.0.0",
  "description": "",
  "scripts": {
    "build": "tsc --project tsconfig.json",
    "start": "node dist/index.js",
    "dev": "export BACKUP_START_TIME=1:15; export DB_URL=postgresql://postgres:password@localhost:5432/postgres; export S3_ACCESS_KEY_ID=; export S3_SECRET_ACCESS_KEY=; export S3_ENDPOINT_URL=; export S3_BUCKET=; ts-node src/index.ts"
  },
  "license": "ISC",
  "devDependencies": {
    "@types/node": "^17.0.23",
    "mocha": "^9.2.2",
    "ts-node": "^10.7.0",
    "typescript": "^4.6.3"
  },
  "dependencies": {
    "aws-sdk": "^2.1107.0"
  }
}
